// Variables

void setup(){

}

void loop(){
  // If no command -> do nothing
  // Else -> do_command(path)
}

void do_command(){
  /*
  for (x,y) in path 
    reached_1 & reached_2 == false
    determine if current RE is < or > than desired
    while (not_satisfied){
      if current RE tick <> = x{
        run motor 1
      }
      else{
        stop motor 1
        reached_1 = true
      }

      if current RE 2 tick <> = y{
        run motor 2
      }
      else{
        stop motor 2
        reached_2 = true
      }

      if reached_1 & reached_2
        satisfied = true
          -> satisfied = true
          -> while breask, next for runs
          -> next point on path
    }
  */
}